import 'package:flutter/material.dart';

class Lupakatalalauan extends StatelessWidget {
  const Lupakatalalauan({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Lupa Katalalauan'),
      ),
      body: const Center(
        child: Text('Lupa Katalalauan'),
      ),
    );
  }
}