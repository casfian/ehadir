import 'package:flutter/material.dart';

class Daftarkeluar extends StatelessWidget {
  const Daftarkeluar({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Daftar Keluar'),
      ),
      body: const Center(
        child: Text('Daftar Keluar'),
      ),
    );
  }
}
